# Set at Docker build time via docker-compose.yml build args.
# For local dev only — in production these are injected by the Dockerfile ARGs.
NEXT_PUBLIC_API_BASE_URL=https://yourdomain.com
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your_publishable_key
